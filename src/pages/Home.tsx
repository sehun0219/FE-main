import BaseLayout from "@/components/common/BaseLayout";
import Navbar from "@/components/common/Navbar";

const Home = () => {
  return (
    <BaseLayout>
      <header>
        <Navbar />
      </header>
    </BaseLayout>
  );
};

export default Home;
